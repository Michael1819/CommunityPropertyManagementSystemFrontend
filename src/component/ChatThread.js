import React from "react";
import {Layout} from "antd";

const {Header, Content, Sider} = Layout;

const ChatThread = () => {
    return (
        <Content className="site-layout-background" style={{padding: 24, margin: 0, minHeight: 280}}>{/* Your content here */}</Content>
    );
};

export default ChatThread;
